'''
DESCRIPTION
asks the user if they want to override oldBooking with newBooking,
    if so, calls override() on oldBooking, book the newBooking, and returns "overrode"

    else, do nothing and returns "no override" 

RETURN VALUE
return either "overrode" or "no override" 
''' 

def overrideBooking(user, oldBooking, newBooking, bookingLog):
    return "overrode"